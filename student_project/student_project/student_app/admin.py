from django.contrib import admin
from .models import Student

admin.site.register(Student) # This line is used to register the model in the admin panel. It is used to display the model in the admin panel and to perform CRUD operations on the model.

# Register your models here.
